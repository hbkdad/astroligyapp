interface:
  display_name: "Database Migration"
  short_description: "Plan and verify safe database migrations"
  default_prompt: "Use $database-migration to implement and verify this schema change safely."
